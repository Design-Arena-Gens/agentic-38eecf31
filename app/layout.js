import './globals.css';

export const metadata = {
  title: 'Bildanalys (LLM) ? agentic-38eecf31',
  description: 'Fota eller ladda upp bilder och f? AI-analys',
};

export default function RootLayout({ children }) {
  return (
    <html lang="sv">
      <body>{children}</body>
    </html>
  );
}
